django-impersonate user users impersonation impersonating impersonated impersonator set setting version settings-format default defaults use installation install installs installed superuser versions-note urls url allow-superusers new-functionality list listing search searching path option optionally options optional session sessions signals signal function support supporting fields-searched dictionary admin allowed allows adds add supported-environments need python test testing tests example items checkout grained page value field old request environment sql-lookup number classes class returns return returned attribute