version-query automation continous integration git releasing semantic versioning tagging version-query-alt-package code packaging packages python releases release current currently queried compatible follow following followed repository incremented incrementing commits number numbers additionally addition tags tag integer-increments characters test tests tested api commit-sha script example examples arbitrary relaxed os assumed separates component components requirements requires line base generated-based information explicit explicitly results result resulting metadata