djangular reusable django app provides better integration tools angular js angularjs use-apps templates templating included includes including include template-like url urls current static requirements-currently inside constant content requests request route rest csrf allows allow tests test requires note modules module needs need structures-needed structure features feature uses testing-installation make provided provide fairly way json different-ways end ending ends files file token css domain installs install installed configure configuration