braces python rendering renderer version versions versioned templating template tests tested test class directory unicode logic view-classes use uses mustache json values encoding encoded installed install installing installation source-e file-encodings page files escape default defaults simplejson string strings single distribution distribute ctemplate pystache render rendered added longer supporting supported supports sections bugfix spec copyright json-support adds add section-use listed list lists attribute value issue context contexts packages package requirements require pass-attributes return returning removed enaeseth escape-function include including included functionality requires-simplejson chris documentation notice templatespec passes passing fixes fix process processed tox